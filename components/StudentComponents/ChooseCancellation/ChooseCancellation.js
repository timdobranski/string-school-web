import styles from './ChooseCancellation.module.css';

export default function ChooseCancellation({ student }) {

    return (
      <div className='infoCard'>
        <h1>{`Choose which upcoming lesson(s) you'd like to cancel`}</h1>
      </div>
    )

}